version: 0.2

phases:
  install:
    commands:
      - bundle install --deployment

  build:
    commands:
      # Run the tests using RSpec.
      - bundle exec cucumber
  post-build:
    commands:
      - tar -czf project.zip .

artifacts:
  type: zip
  files:
    - project.zip